<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MauiCamera.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModels="clr-namespace:MauiCamera.ViewModels"
    x:DataType="viewModels:SettingsPageViewModel"
    Title="SettingsPage">
    <VerticalStackLayout>
        <Button Text="Pick main folder..." Command="{Binding PickFolderCommand}" />
        <Label Text="{Binding FolderName}" />
        <Label Text="{Binding FolderPath}" />
    </VerticalStackLayout>
</ContentPage>